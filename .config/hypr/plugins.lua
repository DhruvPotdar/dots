-- plugins.lua — Plugin configuration
-- Hyprexpo (workspace overview)

-- hl.config({
--     plugin = {
--         hyprexpo = {
--             columns         = 3,
--             gap_size        = 5,
--             bg_col          = "rgb(000000)",         -- Match kitty background
--             workspace_method = "first 1",            -- [center/first] [workspace]
--             enable_gesture  = false,                 -- Laptop touchpad, 4 fingers
--         },
--     },
-- })

-- Note: Hyprbars plugin colors are configured in colors.lua
